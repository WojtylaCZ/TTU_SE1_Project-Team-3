<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ttu_se1_project_team_3" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity">Welcome</activity>
        <activity android:name=".LoginActivity">Login</activity>
        <activity android:name=".HomepageActivity">Homepage</activity>
        <activity android:name=".TemplateDetailsActivity">Template Details</activity>
        <activity android:name=".CreatePreStudyFieldsActivity">Create Pre-Study Fields</activity>
    </application>

</manifest>
