<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ttu_se1_project_team_3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.WelcomeActivity">
            Welcome
        </activity>
        <activity android:name=".activities.LoginActivity">
            Login
        </activity>
        <activity android:name=".activities.HomepageActivity">
            Homepage
        </activity>
        <activity android:name=".activities.TemplateDetailsActivity">
            Template Details
        </activity>
        <activity android:name=".activities.CreatePreStudyFieldsActivity">
            Create Pre-Study Fields
        </activity>
        <activity android:name=".activities.ForgotPassword">
            Forgot password
        </activity>
        <activity android:name=".activities.CreateAccount"></activity>
    </application>

</manifest>
